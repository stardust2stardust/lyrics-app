import React from "react";

const SongCard = ({}) => {
  return (
    <div className="text-primary p-5 border-2 border-primary">
      <h3>ARTIST NAME</h3>
      {/* <h2>Name: {artist.artist_name}</h2>
      <p>ID: {artist.artist_id}</p>
      <button
        onClick={getAlbums}
        className="bg-primary text-background p-2">
        More Info
      </button> */}
    </div>
  );
};

export default SongCard;
